Bugs:

*Excessive memory usage as we store the whole sites in memory when we want to cache it. Use cache files instead!!
*mangable's manga list will only show popular, need to tweak the search a bit. (Gamaran won't show for example)
*all mangas aren't found - maybe add another manga site? (mangareader)

